import { FolderForPipe } from './folder-for.pipe';

describe('FolderForPipe', () => {
  it('create an instance', () => {
    const pipe = new FolderForPipe();
    expect(pipe).toBeTruthy();
  });
});
